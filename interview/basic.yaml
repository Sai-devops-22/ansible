# - name: basic
#   hosts: frontend
#   tasks:
#     - name: ping the server
#       ansible.builtin.ping:

- name: printing
  hosts: frontend
  tasks:
    -name: hello
    ansible.builtin.debug:
      msg: "hello world"
