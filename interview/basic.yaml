# - name: basic
#   hosts: frontend
#   tasks:
#     - name: ping the server
#       ansible.builtin.ping:

- name: printing hello
  hosts: local
  connection: local #it won't ask password
  tasks:
    ansible.builtin.debug:
      msg: "hello world"