# - name: conditions
#   hosts: local
#   connection: local
#   vars:
#     NUMBER: 10
#   tasks:
#     - name: print the number is less than 10
#       ansible.builtin.debug:
#         msg: "the {{NUMBER}} is not less than 10"
#       when: NUMBER < 10

#     - name: print number
#       ansible.builtin.debug:
#         msg: "the {{NUMBER}} is greater than 10"
#       when: NUMBER > 10

- name: conditions demo
  hosts: local
  connection: local
  vars:
    NUMBER: 78
  tasks:
  - name: print if the number is less than or equal to 100
    ansible.builtin.debug:
      msg: "Given number {{ NUMBER }} is less than or equal to 100"
    when: NUMBER <= 100

  - name: print if the number is greater than 100
    ansible.builtin.debug:
      msg: "Given number {{ NUMBER }} is greater than 100"
    when: NUMBER > 100